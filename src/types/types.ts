interface fileConfig {
    filename: string,
    minLineLength?: number,
    maxLineLength?: number,
    minLinesCounter?: number,
    maxLinesCounter?: number,
    minFileSize?: number,
    maxFileSize?: number,
}

export default fileConfig